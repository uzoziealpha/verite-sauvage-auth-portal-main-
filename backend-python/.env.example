# RPC endpoint (Ganache/Hardhat node)
RPC_URL=http://127.0.0.1:8545

# Optional explicit contract address override (otherwise auto-loaded from artifact networks[netId].address)
CONTRACT_ADDRESS=

# Path to contract artifact (relative to this backend root)
CONTRACT_ARTIFACT=contracts/FakeProdDetector.json

# CORS (frontend dev server)
CORS_ORIGINS=http://localhost:5173

# Base URL (used in QR deep links)
BACKEND_BASE_URL=http://localhost:4000
